# Java 代码编写约定

在本项目中新增或修改 Java 代码时，应尽可能充分地添加中文注释，让读者无需反复追踪实现也能理解代码的职责、流程和设计原因。请遵循以下要求：

- 为每个类、接口、枚举及其构造方法编写 Javadoc，说明职责、用途、生命周期和必要的使用约束；内部类和匿名类附近也应说明其作用。
- 尽可能为字段、常量、枚举值及重要的局部变量添加注释，说明含义、单位、取值范围、默认值或状态约束。
- 尽可能为每个方法编写 Javadoc，包括私有方法和简单的 getter、setter；说明行为、参数、返回值、可能抛出的异常及副作用。含义显然且没有额外约束的内容可以简写，但不要因此省略整段方法说明。
- 对方法中的主要处理步骤、分支条件、循环、状态变化、异常处理和边界情况添加行间注释，尤其说明为什么这样处理以及各步骤之间的关系。
- 对游戏机制、配方规则、数值换算、客户端与服务端交互等项目特有逻辑，补充足够的背景说明和使用限制。
- 注释使用准确、清晰的中文，与实现保持一致。尽可能提高注释覆盖率，同时避免逐字复述代码、无意义的重复或过时说明。
- 修改现有代码时，同步更新受影响的 Javadoc 和行间注释，并为修改涉及的缺少说明的逻辑补充注释。

# 验证要求

- 对当前项目的代码修改，只需通过编译验证；智能体无需启动游戏进行运行测试。
- 游戏内的运行与功能测试由用户自行完成。
